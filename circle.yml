machine:
  xcode:
    version: "8.0"

dependencies:
  override:
    - git submodule init
    - git submodule foreach --recursive 'git fetch --tags'
    - git submodule update --recursive

test:
  override:
    - bin/test
