machine:
  xcode:
    version: "8.0"

dependencies:
  override:
    - git submodule foreach --init --recursive 'git fetch --tags'
    - git submodule update --init --recursive

test:
  override:
    - bin/test
